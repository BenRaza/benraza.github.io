{
  "name": "portfolio",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "push-dir --dir=dist --branch=gh-pages --cleanup",
    "all": "vite build && push-dir --dir=dist --branch=gh-pages --cleanup"
  },
  "dependencies": {
    "push-dir": "^0.4.1",
    "vue": "^3.3.11",
    "vue-particles": "^1.0.9",
    "vue-router": "^4.2.5"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^4.5.2",
    "vite": "^5.0.10"
  }
}
